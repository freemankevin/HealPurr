---
import { getCollection } from 'astro:content';
import Header from '../../components/Header.astro';
import Timeline from '../../components/Timeline.astro';
import CatSelector from '../../components/CatSelector.astro';
import { getCatById } from '../../config/cats';

const catId = 'tabby';
const cat = getCatById(catId)!;

const allRecords = await getCollection('cats', ({ data }) => data.cat === catId);
const sortedRecords = allRecords.sort((a, b) =>
  b.data.date.getTime() - a.data.date.getTime()
);
---

<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>九儿的健康档案 - HealPurr</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Noto+Sans+SC:wght@400;500;700&family=ZCOOL+KuaiLe&family=Long+Cang&display=swap" rel="stylesheet">
</head>
<body class="bg-gradient-to-br from-slate-900 via-purple-950/30 to-slate-900 text-white min-h-screen">
  <Header />

  <main class="max-w-6xl mx-auto px-8 py-16">
    <div class="mb-16">
      <h1 class="text-5xl font-bold mb-6 flex items-center gap-4 font-cute">
        <div class="relative">
          <img src={cat.svg} alt={cat.name} class="w-14 h-14 drop-shadow-lg" />
          <div class="absolute inset-0 bg-purple-400/20 rounded-full blur-xl"></div>
        </div>
        <span class="bg-gradient-to-r from-pink-400 via-purple-400 to-pink-400 bg-clip-text text-transparent">{cat.name}的健康档案</span>
      </h1>
      <p class="text-xl text-gray-400 font-cute-light">共 {sortedRecords.length} 条医疗记录 🐾</p>
    </div>

    <CatSelector activeCat={catId} />

    <Timeline records={sortedRecords} />
  </main>
</body>
</html>

<style>
  .font-cute {
    font-family: 'ZCOOL KuaiLe', 'Long Cang', 'Noto Sans SC', cursive;
  }
  
  .font-cute-light {
    font-family: 'Long Cang', 'Noto Sans SC', cursive;
  }
</style>
