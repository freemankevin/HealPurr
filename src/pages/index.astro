---
import { getCollection } from 'astro:content';
import Header from '../components/Header.astro';
import Timeline from '../components/Timeline.astro';
import CatSelector from '../components/CatSelector.astro';
import Footer from '../components/Footer.astro';

const allRecords = await getCollection('cats');
const sortedRecords = allRecords.sort((a, b) =>
  b.data.date.getTime() - a.data.date.getTime()
);
---

<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>HealPurr - 猫咪健康档案</title>
  <link rel="stylesheet" href="/styles/fonts.css">
</head>
<body class="bg-gradient-to-br from-purple-50 via-pink-50/60 to-purple-100 text-purple-900 min-h-screen dark:from-slate-900 dark:via-purple-950/30 dark:to-slate-900 dark:text-white relative overflow-hidden">
  <Header records={allRecords} />

  <main class="max-w-7xl mx-auto px-8 py-16">
    <div class="mb-20 text-center animate-float">
      <h1 class="text-6xl md:text-7xl font-bold mb-8 font-cute bg-gradient-to-r from-pink-600 via-purple-600 to-pink-600 bg-clip-text text-transparent animate-gradient drop-shadow-2xl dark:from-pink-400 dark:via-purple-400 dark:to-pink-400">
        猫咪健康档案
      </h1>
      <p class="text-2xl text-purple-700 font-cute-light tracking-wide mb-8 dark:text-gray-300">记录每一次就诊，守护每一份健康 🐾</p>
      <div class="flex justify-center gap-12 text-base text-purple-800 dark:text-gray-400">
        <div class="flex items-center gap-3 bg-purple-200/60 px-6 py-3 rounded-full border border-purple-500/50 backdrop-blur-sm dark:bg-purple-900/30 dark:border-purple-500/30">
          <div class="w-3 h-3 bg-purple-600 rounded-full animate-pulse shadow-lg shadow-purple-600/50 dark:bg-purple-400 dark:shadow-purple-400/50"></div>
          <span class="font-medium font-cute">共 <span class="font-cute-number">{sortedRecords.length}</span> 条记录</span>
        </div>
        <div class="flex items-center gap-3 bg-pink-200/60 px-6 py-3 rounded-full border border-pink-500/50 backdrop-blur-sm dark:bg-pink-900/30 dark:border-pink-500/30">
          <div class="w-3 h-3 bg-pink-600 rounded-full animate-pulse shadow-lg shadow-pink-600/50 dark:bg-pink-400 dark:shadow-pink-400/50"></div>
          <span class="font-medium font-cute"><span class="font-cute-number">3</span> 只可爱的小猫</span>
        </div>
      </div>
    </div>
    
    <div class="mb-16">
      <CatSelector activeCat={null} />
    </div>
    
    <Timeline records={sortedRecords} />
  </main>

  <Footer />
</body>
</html>

<style>
  @keyframes gradient {
    0%, 100% {
      background-position: 0% 50%;
    }
    50% {
      background-position: 100% 50%;
    }
  }
  
  @keyframes float {
    0%, 100% {
      transform: translateY(0px);
    }
    50% {
      transform: translateY(-10px);
    }
  }
  
  .animate-gradient {
    background-size: 200% auto;
    animation: gradient 4s ease infinite;
  }
  
  .animate-float {
    animation: float 3s ease-in-out infinite;
  }
</style>